# ===============================
# DATABASE CONFIGURATION FOR RENDER
# ===============================
# CONFIGURACIÓN MANUAL con los datos de tu DB
spring.datasource.url=jdbc:postgresql://dpg-d4hgoqf5r7bs73brqbe0-a.frankfurt-postgres.render.com:5432/petshop_db_7l3a?sslmode=require
spring.datasource.username=petshop_db_7l3a_user
spring.datasource.password=OeJWo9YHg30G7uQVkHItra9vHyxsOAw6
spring.datasource.driver-class-name=org.postgresql.Driver

# Forzar dialecto y deshabilitar metadata
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Hikari Connection Pool (optimizado para Render)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.idle-timeout=300000

# ===============================
# JPA & HIBERNATE
# ===============================
# IMPORTANTE: Primero usa CREATE, luego cambia a UPDATE
spring.jpa.hibernate.ddl-auto=create
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ===============================
# SERVER CONFIGURATION
# ===============================
server.port=${PORT:8080}
server.servlet.context-path=/

# ===============================
# EMAIL CONFIGURATION - CORREGIDO
# ===============================
# OPCIÓN 1: SSL con puerto 465 (RECOMENDADO para Render)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.protocol=smtps
spring.mail.username=${EMAIL_USERNAME:pulidoibarra15@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:izpv kiec omjh thfu}

# Configuración SSL para puerto 465
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2,TLSv1.3

# Deshabilitar STARTTLS (usamos SSL directo)
spring.mail.properties.mail.smtp.starttls.enable=false

# Timeouts más largos
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000

# Configuración adicional SSL
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false

# ===============================
# APPLICATION CONFIG
# ===============================
app.base-url=${RENDER_EXTERNAL_URL:https://petshop-1-8sbb.onrender.com}
app.upload.dir=./uploads

# ===============================
# THYMELEAF (TEMPLATES)
# ===============================
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html
spring.thymeleaf.cache=false

# ===============================
# LOGGING
# ===============================
logging.level.io.bootify.pet_shop=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.springframework.jdbc=INFO
logging.level.org.springframework.mail=DEBUG

# ===============================
# PRODUCTION SETTINGS
# ===============================
spring.devtools.livereload.enabled=false
spring.devtools.restart.enabled=false

# ===============================
# FILE UPLOAD CONFIGURATION
# ===============================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.file-size-threshold=2KB

# Configuración de recursos estáticos
spring.web.resources.static-locations=classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/,file:${app.upload.dir}/
spring.web.resources.cache.period=3600
spring.web.resources.chain.cache=true

# ===============================
# GEOCODING CONFIGURATION
# ===============================
app.geocoding.nominatim.url=https://nominatim.openstreetmap.org
app.geocoding.nominatim.user-agent=PetShopApp/1.0
app.geocoding.nominatim.limit=5
app.geocoding.nominatim.country-codes=co
app.geocoding.rate-limit.requests-per-second=1
app.geocoding.timeout=10000
app.geocoding.validation-enabled=true

# ===============================
# CORS CONFIGURATION
# ===============================
app.cors.allowed-origins=${ALLOWED_ORIGINS:https://petshop-1-8sbb.onrender.com}
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.allow-credentials=true

# ===============================
# SECURITY CONFIGURATION
# ===============================
# Para producción, considera aumentar estos valores
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000